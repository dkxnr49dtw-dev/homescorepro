# Netlify configuration for React app
[build]
  publish = "."
  command = "echo 'Using pre-built files'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
